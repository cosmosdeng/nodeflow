# TODO.md — 待办清单

> 优先处理 P0(正确性问题) → P1(体验问题) → P2(规划增强)。

## P0 · 正确性缺陷

- [x] **塌缩状态删除子节点后清理悬空外部连线**(已完成)
  - 修复:`graphStore.ts` 新增 `edgeReferencesNode` 辅助函数,`deleteNode` 与 `onNodesChange` 删除分支在过滤连线时解码 `cid:` 端口,引用被删子节点的悬空连线一并清理;删除后统一 `refreshCompositeHidden` 刷新聚合端口与隐藏状态。

- [x] **聚合端口快照与实时计算的一致性**(已完成,展示侧)
  - 修复:`PropertiesPanel.tsx` 聚合输入/输出改为实时调用 `computeCompositePorts`,与画布展示统一为单一数据源。
  - 遗留(后续清理):`collapseComposite` / `refreshCompositeHidden` 仍向 `data.inputs/outputs` 写入快照,现仅作数据冗余与导出兼容,可逐步移除。

- [x] **内部画布新建节点绕开历史记录**(已完成)
  - 修复:新增 store action `addNodeToComposite(compositeId, position)`,原子完成「创建节点 + 加入 childIds + 塌缩态隐藏」并一次性记录历史;`FlowCanvas` 内部画布双击新建改走该 action,不再 `setState` 直改 `hidden`。

## P1 · 体验与健壮性

- [x] **展开态组合节点的对外连线能力**(已完成)
  - 修复:`FlowNodeComponent` 展开态(虚线框)左右边缘渲染聚合端口 Handle,端口 id 与塌缩态一致(`cid:` 编码),可直接对外连线;塌缩 / 展开逻辑天然兼容(展开态聚合连线指向组合节点,塌缩不改写,再展开解码还原)。
  - 说明:展开态聚合端口连线端点落在虚线框边缘,塌缩后保持指向组合节点聚合端口,再展开还原到内部子节点端口。

- [x] **弹窗快照过期提示增强**(已完成)
  - 修复:`CompositePopupView` 监听 `storage` 事件实时刷新快照(主窗口重新弹出覆盖快照时自动同步),并增加 2s 轮询兜底,避免遗漏存储事件;不再需要手动回主窗口重建。

- [x] **组合节点创建时的 actor 与标题**(已完成)
  - 修复:`graphStore` 新增 `majorityActor` 按子节点多数派推断执行主体;`groupSelected` 生成的组合节点标题为「{主体}协作流程(N)」(平手按 human → machine → hybrid 优先级)。

- [x] **聚合端口重名区分**(已完成)
  - 修复:`computeCompositePorts` 对同一方向内重名的端口按「节点名.端口名」降级命名,端口 id 仍为 `cid:` 编码不受影响。

- [x] **撤销 / 重做内部画布标签联动**(已完成)
  - 修复:`GraphSnapshot` 增加 `compositeTabs` / `activeTabId`;`markHistory` 记录标签状态,`undo` / `redo` / `jumpTo` 通过新增的 `restoreTabsFromSnapshot` 完整恢复标签页;旧快照(无标签字段)回退为按节点过滤,保证任何历史跳转都不会残留指向不存在组合的标签页。

## P2 · 规划增强

> 实现顺序建议:先视觉样式(1-2)→ 再基础交互(3)→ 测试加固(4)→ 布局算法(5)→ 组合结构(6-7)→ 全局架构(8)。先建立测试基线(4)再引入新算法(5),符合「由简到繁、由局部到全局」的递进。

- [x] **执行主体颜色外框标识**(已完成)
  - 人、机器、人机协同三类节点使用**不同颜色的外框**标识,提高画布可读性:
  - **人节点** — 黄色线框; **机器节点** — 蓝色线框; **人机协同节点** — 绿色线框。
  - 实现:`ACTOR_META` 新增 `frame` 字段;`FlowNodeComponent` 根元素加 `nf-actor-*` 类;`global.css` 着色并带同色柔和光晕,选中态以 accent 优先;组合节点(塌缩/展开)同样按执行主体着色。

- [x] **组合节点样式定制**(已完成)
  - 组合节点采用**精致化默认样式**并自动继承内部节点属性,不做用户手动配置:
  - **执行主体继承**:`lib/composite.ts` 新增 `computeCompositeActor` —— 内部全为同一主体显示该主体,混杂显示人机协同;组合节点图标 / 边框颜色均随内部节点实时继承。
  - **边框颜色**:自动取继承的执行主体色(人-黄 / 机器-蓝 / 人机协同-绿),机器用更纯正的蓝(#2f6fe0);光晕颜色由边框最终颜色决定,并含向内(inset)扩散光晕;边框加粗(塌缩 4px / 展开 3px)。
  - **折叠动画**:展开 / 塌缩切换时淡入 + 轻微缩放过渡。
  - **选中态**:不改变边框颜色(避免与执行主体色混淆),改为提亮边框 + 增强外圈 / 向内光晕。

- [x] **端点(端口)删除与撤销恢复**(已完成)
  - 节点至少保留一个输入与一个输出端点(每方向 ≤1 时禁止删除)。
  - 实现:新增 `removePort` store action —— 删除端口并同步删除关联连线(含塌缩聚合端口的 `cid:` 引用),`markHistory` 使撤销可同时恢复端口与连线。
  - **删除入口**:普通节点端口(该方向 ≥2 时)悬停出现删除图标(紧贴文字 label 一侧);连线说明 / 中间产物悬停时右侧出现删除图标。
  - **锁定隐藏**:节点锁定或全部锁定时,端口 / 连线说明 / 中间产物的删除图标均不出现。

- [x] **单元测试覆盖核心纯函数**(已完成)
  - 引入 vitest(`test` / `test:watch` 脚本)。
  - `lib/composite.ts`:端口编码 / 解码(含多层链式 `decodeCompositePortPath`)、`computeCompositeActor`(含嵌套递归)、`computeCompositePorts`(排除内部连线 / 重名降级 / 嵌套递归展平)、`computeCompositeBounds`、`getNodeSize`。
  - graphStore:编组塌缩(隐藏子孙 / 外部连线改写)、展开(恢复子节点 / 连线还原)、解除编组(删除组合 / 连线正确还原)状态机。

- [x] **自动布局(全画布 + 组合内对齐)**(已完成)
  - **触发入口**:工具栏新增「自动排列」按钮 + 横向 / 竖向**互斥**方向按钮;选中组合节点时对其内部排列,否则全画布。
  - **算法**:新增 `lib/layout.ts` —— `computeLevels` 按连线依赖拓扑分层(最长路径,环安全);`computeLayout` 按层排列(横向列 / 竖向行),同层节点用 **barycenter 启发式排序**(邻居对齐减少交叉与压节点);默认间距按方向区分,主方向留足空间给连线 label / 中间产物。
  - **作用范围**:全画布(当前可见节点)或组合直接子节点;布局后重算展开态组合虚线框;`markHistory` 撤销可用。
  - **连线层级**:默认节点在连线之上(节点清晰);拖拽节点时临时把连线 / label 提升到节点之上(方案 C),便于看清连线关系。
  - 单元测试 `layout.test.ts`:拓扑分层 / 横向 / 竖向 / 同层不重叠。

- [x] **组合节点嵌套组合**(已完成)
  - 放开 `groupSelected` 限制,支持把已有组合节点编入新组合(嵌套组合)。
  - 端口引用支持多层链式(`cid:A:cid:B:port`,新增 `decodeCompositePortPath`);聚合端口递归展平(外层组合透出内层聚合端口)。
  - 递归显隐:塌缩外层隐藏全部子孙,展开时内层仍塌缩的组合其子节点保持隐藏(`collectAllDescendants` / `refreshCompositeHidden`)。
  - 连线递归改写/还原:塌缩时外部连线改写到外层聚合端口(`wrapRefToOuter`),展开时逐层还原到当前可见层(`unwrapToVisible`)。
  - 执行主体递归继承:嵌套组合的 actor / 边框颜色递归取内层组合的继承结果。

- [x] **复制 / 粘贴节点与组合**(已完成)
  - 新增 `copySelection` / `pasteClipboard` store action + 全局子图剪贴板(`clipboard`),支持 `Ctrl/⌘+C`、`Ctrl/⌘+V` 快捷键与右键菜单「复制 / 粘贴」。
  - **跨画布 / 跨文档复制粘贴**:剪贴板为全局共享,可在**同一项目不同画布标签页**之间、以及**不同项目的画布**之间复制粘贴;粘贴定位到当前视口中心或右键位置。
  - **组合递归复制子图**:复制组合节点时一并复制内部子节点与内部连线,重新生成节点 id / 端口 id / 组合 `childIds`,并递归重映射连线 handle(含 `cid:` 嵌套链);粘贴后 `refreshCompositeHidden` 重算聚合端口。
  - 单元测试:`copySelection` / `pasteClipboard`(含组合递归复制、id / 端口 / handle 重映射)、`onNodesChange` select 与 `copySelection` 联动。

- [x] **多画布 / 多文档**(已完成)
  - **完整多文档模型**:store 重构为 `documents[] + activeDocumentId`,每个文档独立 `nodes/edges/viewport/组合标签/历史栈`;通过活动文档镜像字段(保留顶层 `nodes/edges` 等)兼容现有 action,set 自动同步回文档。
  - **按文档隔离历史**:每个文档各自 `past/future`,切换后撤销不互相污染。
  - **按文档独立持久化**:文档注册表(`nodeflow:docs:v1`)+ 每文档独立 key;启动迁移旧单文档为默认文档。
  - **多标签页界面**:工具栏下方标签栏 —— 项目文档标签(带颜色点,不同项目不同色) + 项目内主画布 / 组合内部画布标签(与主画布相邻);项目标签与项目内标签用分隔线区分。
  - **文档管理**:新建 / 切换 / 关闭(保留 ≥1)/ 重命名。

- [x] **保存项目与文件菜单重组**(已完成)
  - **保存项目**:`serializeProject` 把当前项目完整编辑状态(含撤销/重做历史、组合标签、viewport)序列化为 `.nodeflow` 项目文件;`loadProject` 从项目文件恢复为新文档(含历史),下次打开回到上次编辑位置。
  - **文件菜单重组**:新增「新建项目 / 打开项目 / 保存项目 / 关闭项目」;「导入」子菜单(导入 JSON);「导出」子菜单(导出 JSON / JPG / PDF / SVG,幅面可选 A5/A4/A3/Letter,默认横版,完整画布居中适配);删除原「新建文档」。
  - **导出实现**:JPG/PDF 用 `html-to-image` 截取整个画布(保留主题背景色 + 点阵,隐藏 MiniMap/Controls);SVG 用**数据驱动矢量重绘**(`exportSvg.ts` 读取 nodes/edges 用 SVG 原生元素绘制),当前暂置灰待完善。
  - **关闭项目**:标签栏 × 与文件菜单均可关闭,关闭时若有未保存修改则提示保存(`confirmAndCloseDocument`);关闭唯一项目时自动新建空项目替换;新增 `saveDocument(id)` 保存指定文档。

- [x] **项目文件关联(桌面端)**(已完成)
  - electron-builder `fileAssociations` 注册 `.nodeflow` 扩展名并绑定主程序图标;主进程处理双击打开(macOS `open-file` / Win/Linux 单实例 `second-instance`+argv),读取文件内容经 IPC 推送,渲染进程 `loadProject` 自动打开;preload 暴露 `window.nodeflow.onOpenProjectFile`。

- [x] **新对象创建默认进入编辑模式**(已完成)
  - 新节点创建 → 自动进入节点名称编辑态;新连线创建 → 画布上连线说明自动进入编辑态;新输入/输出端点创建 → 自动进入端点 label 编辑态。共用 `EditableText`(提取为共享组件)的 `autoFocus` 机制。

- [x] **节点标题长文本换行**(已完成)
  - 节点标题不再单行省略号截断,改为允许换行(不改变节点宽度,多行显示)。

## P3 · 进阶能力

> 实施顺序:① 注释框 → ② 流程阶段域 → ③ 逻辑判断/分支节点。依赖链 P3-1 → P3-2 → P3-3;由简到繁、先基础能力(注释)再结构(域)再业务节点(分支),每阶段可独立验证交付。
> 难度评估:P3-1 中高 / P3-2 中 / P3-3 中。

- [x] **注释框**(P3-1,已完成)
  - 注释框含**标题 + 内容**,**半透明、点状虚线边框、无背景**,可归属 **画布 / 节点 / 连线 / 连线产物 / 组合节点**(组合节点展开 / 塌缩均可)。
  - **画布归属**:独立 overlay 层,可自由拖拽摆放。
  - **红色 pin 图标**:无注释时**悬停显示**(节点 / 产物 / 组合在正下方,连线在说明上方,带延迟消失便于点击);有注释收起时**持续显示**;展开时不显示。一个主体最多一个注释。
  - **编辑**:新建注释**默认进入标题编辑**,单击内容进入编辑,双击注释框进入编辑;点其它地方保持展开,点 `–` 收起。
  - **工具栏**:💬 一键全部展开 / 收起;删除需内联二次确认(「删 / 否」)。
  - **解除编组**:组合注释合并到第一个子节点(有注释则合并内容,无则转移)。
  - **序列化**:保存项目 / 导出 JSON 包含注释;删除节点 / 连线自动清理其注释;纳入历史栈 / 多文档隔离;导出 JPEG / PDF 时按当前展开 / 收起状态渲染。
  - **修复**:双击注释不再新建节点、悬停不误触发其它主体注释、编辑文字卡住 draggable 导致节点不可拖、锁定全部时 per-node draggable 覆盖全局锁定等。
  - **连线右键插入节点**(补充,已完成):连线上右键「在此添加节点」——在连线中间插入新节点,原连线拆成两段,原连线的**说明文字 / 中间产物 / 连线归属与产物归属注释全部转移到上游连线**,新节点自动进入标题编辑;配合组合节点右键新增「展开 / 收起」。
  - **展开组合节点内部可交互**(补充,已完成):展开虚线框不再拦截事件,内部节点可正常拖拽 / 从端口拉线 / 选中 / 加删端点。实现:组合节点设 `className: nf-expanded-frame`,CSS 用精确类选择器 `.react-flow__node.nf-expanded-frame { pointer-events: none }` 穿透(仅标题栏 `auto`),并把内部节点 `zIndex` 提升为 1;展开时相邻重叠节点与连线产物自动推开。**注意勿改用 `:has()`(部分环境不兼容)**。

- [x] **流程阶段域**(P3-2,已完成)
  - 矩形**虚线框**域,带**可编辑名称**,内部填充与边框同色(低饱和低明度光谱色,按域水平位置红→蓝紫)。
  - **归属(长按进入)**:节点拖入域框后**继续按住 1 秒**才归属(节点 + 域闪烁反馈);否则只是视觉叠加。
  - 域**完全穿透**:域内节点 / 连线 / 中间产物可执行一切正常操作,不受域影响。
  - **自动扩大 + 外部推挤**:拖拽域内节点时域框按节点包围盒(内边距 22)自动扩大,扩大后把重叠的外部节点 / 组合 / 其他阶段域按最小位移推开(间距 14)。
  - **resize 最小覆盖**:缩小域框最小只能缩到覆盖所有域内节点(含内边距),再小无效。
  - **脱离**:选中一个或多个节点 → 右键「脱离阶段域」;域右键「脱离全部节点」「删除阶段域」;Delete 键 / 右键删除域后内部节点 / 组合自动脱离、连线不变。
  - 拖域移动(四边拖拽条 + 头部)、右下角调整大小;一节点只能属一个域;纳入历史栈 / 多项目 / 序列化。
  - **自动排列集成**:全画布自动排列时——每个域内部节点先横向拓扑排列、域框收敛包裹;单节点域框大小对齐「除自身外最大的域」;所有阶段域框垂直中心水平对齐;各域作为整体块 + 游离节点做全局横向排列。
  - **自动排列仅横向**:移除竖向排列(工具栏去掉方向切换按钮,固定横向)。
  - **交互细节**:新建阶段域自动进入名称编辑;双击名称进入编辑(不再误建节点);新建域自动放画布空白处(不与现有元素 / 域重合);点击空白取消域选中。

- [x] **逻辑判断与分支节点**(P3-3,已完成)
  - **三种核心网关**:排他(XOR,菱形 `×`)/ 并行(AND,菱形 `+`)/ 包容(OR,菱形 `○`)。
  - **分支 + 汇聚都支持**:网关既可分支(一进多出,加输出分支)也可汇聚(多进一出,加输入端口)。
  - **仅可视化语义**:菱形外观 + 条件 label 表达,不真正执行流程。
  - **条件通过出口连线 label 表达**(复用连线说明);支持切换网关类型。
  - 画布右键菜单直接创建三种网关(平铺项);网关上右键切换类型。
  - 网关为控制流节点,菱形外观 + 内部标记,颜色与执行主体色 / 域色协调。
  - 输入 / 输出端点贴菱形边角(输出以右顶点为中轴对称,输入左顶点留间距);左右直角内 `+` 按钮添加端口;删除 `×` 悬停显示;端点 label 白色。
  - 可归属流程阶段域;`FlowNodeData.gateway` 随节点自动序列化;参与自动排列。
